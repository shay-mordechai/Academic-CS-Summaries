
---

## 5. DIP: Dependency Inversion Principle (עקרון )

[[SRP]] | [[OCP]] | [[LSP]] | [[ISP]] | [[DIP]]

עקרון היפוך תלויות [[DIP]], הוא אחד מעקרונות ה-SOLID החשובים ביותר, והוא שווה במידת אזכורו במבחנים ל-[[SRP]] (שניהם עם **11 אזכורים** במצטבר). [[DIP]] הוא העיקרון המשמש לבניית "חומות" ארכיטקטוניות שחוסמות מעבר תלויות לא נכונות.

### הגדרה וחשיבות

**הגדרה:** DIP מוגדר באמצעות שני כללים מרכזיים:

1. מודולים **מופשטים** (`Abstractions`, רמה גבוהה, מדיניות) לא צריכים להיות תלויים בפרטים (Details, רמה נמוכה, מימוש).
    
2. **הפרטים** (`Details`) צריכים להיות תלויים **בהפשטות**.
    

**חשיבות:** העיקרון דורש שמודולים ברמה גבוהה (כמו הלוגיקה העסקית) **לא יהיו תלויים** ישירות במודולים ברמה נמוכה (כמו פרטי מימוש טכניים, חומרה קונקרטית, או כונן קשיח). במקום זאת, **שני המודולים צריכים להיות תלויים בממשק או במחלקה מופשטת משותפת**.

יישום DIP מוביל למערכת **ניתנת להרחבה** (extensible), **ניתנת לשימוש חוזר** (reusable), ו**אינה קשיחה** (non-rigid).

### יישום ותבניות עיצוב

1. הבעיה הקלאסית (תלות בפרטים):

דוגמה להפרה היא כאשר מודול ברמה גבוהה (כמו Copier) תלוי ישירות במחלקות קונקרטיות (פרטים) כמו Keyboard (קלט) ו-Printer (פלט).

2. הפתרון (היפוך התלות):

במקום זאת, Copier יהיה תלוי בממשקים מופשטים כמו IReader ו-IWriter. Keyboard ו-Printer יממשו את ההפשטות הללו. כך, התלות "הופכת": המודול ברמה הגבוהה תלוי בהפשטה, והמודול ברמה הנמוכה תלוי גם הוא באותה הפשטה.

**3. עקרון [[DIP]] במקרה הבוחן [[מערכת ניטור מזג אוויר (WMS)]]:**

- **הפרה ותיקון באמצעות [[Observer]]:** בעיצוב הראשוני של WMS, הייתה תלות של ה-`TemperatureSensor` (מודול מופשט) בפרט קונקרטי כמו `MonitoringScreen`. תלות זו הפרה את DIP, מה שהוביל גם להפרת [[OCP]] (כי הוספת צרכן חדש דרשה שינוי בקוד הקיים). הפתרון הושג באמצעות תבנית **[[Observer]]** שניתקה את התלות הישירה.
    
- **הפרה ותיקון באמצעות [[Bridge]]:** תבנית ה-**[[Bridge]]** נועדה לפתור בעיות [[DIP]] באופן ישיר. ב-WMS, היא שימשה לשחרור התלות בין **שכבת ההפשטה (האפליקציה)** לבין **שכבת המימוש (החומרה)**.
    

---

## 📚 שאלות ממבחנים

- **תשפ"ד א ב שאלה 11:** (מי צריך להיות תלוי בהפשטות).
    
- **תשפ"ד ב א שאלה 5:** (הפרת DIP בגלל תלות במחלקה קונקרטית `Car`).
    
- **תשפ"ד ב ב שאלה 5:** (`Bridge` משמש לפתרון בעיית DIP).
    
- **תשפ"ד ב ב שאלה 3:** (ISP/DIP).
    
- **תשפ"ד א ב שאלה 11:** (חשיבות ה-DIP בתיקון הפרת תלות בין שכבה גבוהה לנמוכה).
    

---

קודם: [[ISP]]

הבא: [[יחידה 3 עקרונות העיצוב חבילות (Packages)]]

חזרה ל: [[יחידה 2 עקרונות העיצוב (SOLID)]]

תוכן עניינים ראשי: [[Summeries/עיצוב/תוכן עניינים]]