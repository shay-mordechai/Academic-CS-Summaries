
---

## 2. OCP: Open Closed Principle (עקרון פתיחות/סגירות)

[[SRP]] | [[OCP]] | [[LSP]] | [[ISP]] | [[DIP]]

עקרון OCP הוא "עקרון מפתח" של תכנות ועיצוב איכותי, והוא מהווה את האסטרטגיה שעל פיה ניתן לתת מענה לדרישות משתנות. העקרונות האחרים של [[יחידה 2 עקרונות העיצוב (SOLID)]] נגזרים ממנו.

### הגדרה וחשיבות

**הגדרה פורמלית:** "תוכנה צריכה להיות **פתוחה להרחבה** ו**סגורה לשינויים**."

- **פתוחה להרחבה:** ניתן להרחיב את התנהגות התוכנה ולאפשר לה לפעול בדרכים חדשות ושונות.
    
- **סגורה לשינויים:** קוד התוכנה הקיים **לא ישתנה** (אפילו לא לצורך קומפילציה מחדש).
    

**חשיבות למבחן:** עמידה ב-OCP היא חיונית משום שהיא מובילה לגמישות גבוהה ולקוד קל יותר לשינוי והרחבה. הפרת OCP הופכת את הקוד ל**קשיח** (rigid) ו**שביר** (Fragile).

### זיהוי הפרות OCP

OCP מופר כאשר **הוספת פונקציונליות חדשה מחייבת שינוי** בקוד קיים.

דוגמה קלאסית: בדיקת טיפוסים קונקרטיים

הפרה קלאסית מתרחשת כאשר פונקציה משתמשת בבדיקת טיפוסים קונקרטיים (instanceof), במקום להסתמך על הפשטות (ממשקים או מחלקות מופשטות).

לדוגמה, בפונקציה `drawAllShapes` המקבלת רשימה של `Shape`:

Java

```
if (s instanceof Square) {
    drawSquare((Square) s);
}
if (s instanceof Circle) {
    drawCircle((Circle) c);
}
```

אם נוסיף צורה חדשה (כמו `Triangle`), נצטרך **לפתוח את הקוד** של הפונקציה `drawAllShapes` ולשנות אותו על ידי הוספת עוד תנאי `if`, ובכך נפר את OCP.

### פתרון (וחיבור לתבניות עיצוב)

הפתרון לעמידה ב-OCP הוא **שימוש בהפשטות** (ממשקים ומחלקות מופשטות). תבניות עיצוב רבות נוצרו כדי לעזור בשמירה על OCP:

- **[[Factory]]**: עוזרת לפתור הפרות OCP הנגרמות מיצירה ישירה של אובייקטים (שימוש ב-`new`).
    
- **[[Visitor]]**: נועדה לפתור הפרה של OCP ו-SRP שנובעת מהצורך להוסיף פעולה חדשה למשפחה של מחלקות, מבלי לשנות את המחלקות הקיימות.
    
- **[[Decorator]]**: מאפשר להוסיף יכולות והתנהגויות חדשות לאובייקט **בזמן ריצה** מבלי לשנות את המחלקה המקורית.
    
- **[[Strategy]]**: מיועדת להחליף אלגוריתמים בצורה גמישה מבלי לשנות את מחלקת הלקוח.
דוגמא 1:
![[Screenshot_20251105_174729.png]]

---

## 📚 שאלות ממבחנים

- **תשפ"ד ב א שאלה 5:** (הפרת OCP בגלל שימוש ב-`instanceof` על Car).
    
- **תשפ"ד ב א שאלה 6:** (אילו מחלקות יצטרכו להיפתח לשינויים בהוספת משתנה חדש).
    
- **תשפ"ד א ב שאלה 1:** (OCP מופר על ידי A, B, C).
    
- **תשפ"ד א ב שאלה 6:** (תיקון OCP באמצעות ממשק משותף).
    

---

קודם: [[SRP]]

הבא: [[LSP]]

חזרה ל: [[יחידה 2 עקרונות העיצוב (SOLID)]]

תוכן עניינים ראשי: [[Summeries/עיצוב/תוכן עניינים]]