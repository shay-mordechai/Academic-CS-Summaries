# תבנית 3: אופרטורים חסומים (Bounded Operators)

תבנית זו מתייחסת לשימוש באופרטורים הסוגרים מחלקה של פונקציות [[רקורסיה פרימיטיבית|ר"פ]]:
* **סכימה חסומה:** $\sum_{t \leq y}$
* **מכפלה חסומה:** $\prod_{t \leq y}$
* **מינימיזציה חסומה:** $\min_{t \leq y}$
* **מקסימיזציה חסומה:** $\max_{t \leq y}$

## 📚 שאלות ממבחנים (T3)

| שאלון/מועד | שאלה | תוכן השאלה/נושא |
| :--- | :--- | :--- |
| **תשפ"א א מועד א** | שאלה 1.א | הוכח ש-$f(n)$ המחזירה את **כמות המספרים הראשוניים הקטנים מ- $n$** היא ר"פ (דורש סכימה חסומה על פרדיקט ראשוניות). |
| **תשפ"א א מועד א** | שאלה 1.ב | הוכח ש-$g(n)$ המחזירה את **כמות הריבועים השלמים** הקטנים מ- $n$ היא ר"פ (דורש סכימה/כמת חסום). |
| **תשע"ו ב מועד א** | שאלה 2 | חישוב/קביעת תחום הגדרה עבור $f(x) = \min_{z \leq x} (\dots)$ ו-$g(x) = \max_{z \leq x} (\dots)$ (שאלות חישוביות ישירות על אופרטורים חסומים). |

---

## 💡 פתרונות מלאים

### 1. תשפ"א סמסטר א' מועד א' - שאלה 1

#### סעיף א': כמות המספרים הראשוניים הקטנים מ-$n$

**הפונקציה:**
$f(n)$ מחזירה את מספר הראשוניים $p$ כך ש-$p < n$.

**פתרון:**
כדי להוכיח שהפונקציה היא [[רקורסיה פרימיטיבית|ר"פ]], נשתמש בפרדיקט הידוע $Prime(x)$, שמחזיר 1 אם $x$ ראשוני ו-0 אחרת.
ידוע כי $Prime(x)$ הוא פרדיקט ר"פ. הפונקציה $f(n)$ סופרת כמה מספרים $i$ בטווח $0 \le i < n$ מקיימים את התכונה שהם ראשוניים. פעולה זו היא בדיוק **סכום חסום** (Bounded Summation) של הפרדיקט המציין.

**הגדרה פורמלית:**
$$f(n) = \sum_{i=0}^{n-1} Prime(i)$$
*(הערה: הסכום רץ עד $n-1$ כי השאלה דורשת ראשוניים הקטנים **ממש** מ-$n$. ניתן להשתמש בסימון המקובל לסכימה חסומה $\sum_{i < n}$).*

**הנימוק להוכחת ר"פ:**
1. הפרדיקט $Prime(x)$ הוא ר"פ.
2. מחלקת הפונקציות הר"פ סגורה תחת **סכימה חסומה**.
3. לכן, $f(n)$ היא פונקציה ר"פ.

#### סעיף ב': כמות הריבועים השלמים הקטנים מ-$n$

**הפונקציה:**
$g(n)$ מחזירה את מספר המספרים $k < n$ שהם ריבועים שלמים (כלומר קיים $x$ כך ש-$x^2 = k$).

**פתרון:**
תחילה נגדיר פרדיקט עזר $Square(k)$ הבודק האם $k$ הוא ריבוע שלם:
$$Square(k) = \exists x \le k [x \cdot x = k]$$

**הסבר למה $Square(k)$ הוא ר"פ:**
* הביטוי $x \cdot x = k$ מורכב מפונקציית הכפל ופרדיקט השוויון, שניהם ר"פ.
* השימוש ב[[כמתים חסומים|כמת "קיים" חסום]] ($\exists x \le k$) שומר על תכונת הר"פ.

כעת נגדיר את $g(n)$ באמצעות סכום חסום על הפרדיקט:
$$g(n) = \sum_{i=0}^{n-1} Square(i)$$

**הנימוק:**
כיוון ש-$Square(i)$ הוא פרדיקט ר"פ, והפונקציה $g(n)$ מוגדרת כסכום חסום שלו, גם $g(n)$ היא ר"פ.
*(לדוגמה: עבור $n=10$, הריבועים הקטנים מ-10 הם 0, 1, 4, 9. הסכום יחזיר 4)*.

---

### 2. תשע"ו סמסטר ב' מועד א' - שאלה 2

**השאלה:**
נתונות שתי פונקציות המוגדרות על ידי [[מינימיזציה חסומה]] ומקסימיזציה חסומה על אותו פרדיקט:

$$f(x) = \min_{t \le x} \Big((t>0) \land \forall z_{\le x} \big((z+t) \ge x \lor \neg ((z+t)|x)\big) \Big)$$
$$g(x) = \max_{t \le x} \Big((t>0) \land \forall z_{\le x} \big((z+t) \ge x \lor \neg ((z+t)|x)\big) \Big)$$

יש לחשב את $f(6)$ ו-$g(6)$.

**ניתוח הפרדיקט:**
נסתכל על התנאי בתוך הכמת:
$$(z+t) \ge x \lor \neg ((z+t)|x)$$
זה שקול לוגית ל: **אם $z+t < x$, אז $(z+t)$ אינו מחלק את $x$.**

כאשר $z$ רץ בטווח $0 \dots x$, הביטוי $z+t$ מכסה את כל המספרים החל מ-$t$ ומעלה.
התנאי דורש ששום מספר בטווח $[t, x-1]$ לא יחלק את $x$.
במילים פשוטות: **$t$ חייב להיות גדול ממש מכל המחלקים הממשיים של $x$ (הקטנים מ-$x$).**

**חישוב $f(6)$:**
1. המחלקים של 6 הם: 1, 2, 3, 6.
2. המחלקים הקטנים ממש מ-6 הם: 1, 2, 3.
3. הפרדיקט דורש שכל מספר בטווח $[t, 5]$ **לא** יחלק את 6.
    * אם $t=1$: הטווח מכיל את 1, 2, 3 (שהם מחלקים). לא מתאים.
    * אם $t=2$: הטווח מכיל את 2, 3. לא מתאים.
    * אם $t=3$: הטווח מכיל את 3. לא מתאים.
    * אם $t=4$: הטווח הוא $[4, 5]$. המספרים 4 ו-5 אינם מחלקים את 6. **התנאי מתקיים**.

המינימום הוא לכן **$f(6) = 4$**.
*(הערה כללית: $f(x)$ מחזירה את (המחלק הקטן-ממש הגדול ביותר של $x$) + 1).*

**חישוב $g(6)$:**
הפונקציה מחפשת את ה-$t$ המקסימלי שמקיים את התנאי.
נבדוק את הגבול העליון של החיפוש, $t=x=6$:
כאשר $t=6$, הטווח $[t, x-1]$ הוא ריק (או בפורמליות: לכל $z \ge 0$, מתקיים $z+6 \ge 6$, ולכן החלק הראשון של ה-"או" $(z+t) \ge x$ תמיד אמת).
לכן התנאי מתקיים עבור $t=6$.

המקסימום האפשרי הוא **$g(6) = 6$**.

**תשובות סופיות:**
* $f(6) = 4$
* $g(6) = 6$

**שאלות המשך:**
* **האם קיים $x>6$ שעבורו $f(x)=x$?**
  **לא.** תמיד 1 הוא מחלק של $x$, ולכן $t$ חייב להיות גדול מ-1. הערך של $f(x)$ חסום בערך ב-$x/2 + 1$, ולכן עבור $x>2$ לא ייתכן שוויון.
* **האם קיים $x>6$ שעבורו $g(x)=x$?**
  **כן, לכל $x$**. כפי שהראינו, $t=x$ תמיד מקיים את התנאי באופן ריק (vacuously true), ולכן המקסימום הוא תמיד $x$.

---
